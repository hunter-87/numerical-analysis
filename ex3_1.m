% function integral

% trapz  Trapezoidal numerical integration.
% Z = trapz(Y) computes an approximation of the integral of Y via
% the trapezoidal method (with unit spacing).  To compute the integral
% for spacing different from one, multiply Z by the spacing increment.
 
        
help trapz

% This MATLAB function approximates the integral of 
% function fun from xmin to xmax using global adaptive 
% quadrature and default error tolerances.

help integral

% integral2 - Numerically evaluate double integral
% This MATLAB function approximates the integral of 
% the function z = fun(x,y) over the planar region 
% xmin ≤ x ≤ xmax and ymin(x) ≤ y ≤ ymax(x).

help integral2

% integral3 - Numerically evaluate triple integral
% This MATLAB function approximates the integral of 
% the function z = fun(x,y,z) over the region 
% xmin ≤ x ≤ xmax, ymin(x) ≤ y ≤ ymax(x) and 
% zmin(x,y) ≤ z ≤ zmax(x,y).

help integral3


% triplequad - Numerically evaluate triple integral 
% This MATLAB function evaluates the triple integral 
% fun(x,y,z) over the three dimensional rectangular 
% region xmin <= x <= xmax, ymin <= y <= ymax, zmin <= z <= zmax.

help triplequad

% quad - Numerically evaluate integral, adaptive 
% Simpson quadrature. This MATLAB function tries to 
% approximate the integral of function fun from 
% a to b to within an error of 1e-6 using recursive 
% adaptive Simpson quadrature.

help quad


% quadl - Numerically evaluate integral, 
% adaptive Lobatto quadratureThis MATLAB function 
% approximates the integral of function fun from a to b, 
% to within an error of 10-6 using recursive adaptive Lobatto quadrature.

help quadl

% dblquad - Numerically evaluate double integral
% over rectangleThis MATLAB function calls the 
% quad function to evaluate the double integral 
% fun(x,y) over the rectangle xmin <= x <= xmax, ymin <= y <= ymax.

help dblquad


% quadgk - Numerically evaluate integral, adaptive Gauss-Kronrod quadrature
% This MATLAB function attempts to approximate the integral 
% of a scalar-valued function fun from a to b using high-order 
% global adaptive quadrature and default error tolerances.

help quadgk

% quad2d - Numerically evaluate double integral over planar region
% This MATLAB function approximates the integral of fun(x,y) 
% over the planar region and .

help quad2d





